// Import yet another module.
// If a path begins with a slash then it will be treated as an absolute path
// relative to the program's main directory.
// The module name should normalize to `test_imports_baz.krox`.
import "/test_imports_baz.krox" {}
