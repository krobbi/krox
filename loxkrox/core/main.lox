// Run LoxKrox from arguments.
fun main(){
	var logger = Logger();
	
	if(_args() == 1){
		run_tests(_arg(0), logger);
	} else {
		logger.log("Expected arguments `krox.lox`.");
	}
	
	logger.flush();
}

main();
