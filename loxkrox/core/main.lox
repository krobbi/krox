// Run LoxKrox from arguments.
fun main(){
	var logger = Logger();
	
	if(_argc() == 1){
		run_tests(_argv(0), logger);
	} else {
		logger.log("Expected arguments `krox.lox`.");
	}
	
	logger.flush();
}

main();
