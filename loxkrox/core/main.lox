// Run LoxKrox from arguments.
fun main(){
	var logger = Logger();
	
	if(x_args() == 1){
		run_tests(x_arg(0), logger);
	} else {
		logger.log("Expected arguments `krox.lox`.");
	}
	
	logger.flush();
}

main();
